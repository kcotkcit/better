#!/usr/bin/env python

from distutils.core import setup

setup(name='redbetter',
      version='1.0',
      description='better.py for red',
      author='anon',
      author_email='fake@fake.com',
      url='https://www.fake.website',
      packages=['redbetter'],
      scripts=['bin/redbetter'],
     )
